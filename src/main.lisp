(in-package :lisp-project-template)

;;; Code goes below. When this file gets too large or you want to break code
;;; into more files to keep it semantically clean and modular, then create other
;;; files and add them to lisp-project-template.asd in the components section.
;;; Also update package.lisp if necessary at that time.
